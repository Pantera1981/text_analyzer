#анализ текста

""" Цель: разработать программу, которая анализирует текстовые файлы и выводит статистику.
Требования:
1.	Подсчет статистики:
o	Программа должна подсчитывать общее количество слов, предложений и символов в текстовом файле.
2.	Частота слов:
o	Определить самые частые слова в тексте и вывести их частоту.
3.	Удобный вывод:
o	Результаты анализа должны быть представлены в удобочитаемом формате.
Дополнительные задачи:
●	Добавить возможность игнорировать общие слова (например, "и", "в", "на").
●	Реализовать возможность сохранять результаты анализа в текстовый файл.
●	Добавить графическую визуализацию (например, с помощью Matplotlib) для отображения частоты слов. """

import collections
import re
 #Загрузить текст
with open('C:/temp/text.txt', 'r',encoding='utf-8') as file:
    text = file.read()
   
   #Подсчет количества слов, символов, предложений
    characters_count = len(text) 
    word = re.findall(r'\b\w+\b',text.lower())
    word_count = len(word)
    sentences = re.split(r'[.!?]+', text.lower())
    sentences_count = len([s for s in sentences if s.strip()])
   
    #Частые слова
    counter = collections.Counter(word)
    most_common_words = counter.most_common(10)
